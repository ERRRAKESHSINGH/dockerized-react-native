# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - agentic-workbench-dev1

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://agenticworkbenchdev1.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_0df407da581742aa92054b46b1471109 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_a40322cdccee4cf5b0cf2855dd0cac9d }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: agenticworkbenchdev1.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_0df407da581742aa92054b46b1471109 }}/workbench-ui:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'agentic-workbench-dev1'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_77c18e6fe60b4d7c95d7f807aeae9680 }}
        images: 'agenticworkbenchdev1.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_0df407da581742aa92054b46b1471109 }}/workbench-ui:${{ github.sha }}'